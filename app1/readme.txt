npm add expo ////instalar o diretorio do expo localmente
"npx expo install now has a --fix flag which installs the correct versions of installed packages. 
You can also provide specific packages to fix like npx expo install expo-sms --fix.
 We’ve also added a --check flag which can be used in CI to fail if packages are outdated."
npx create-expo-app --template
https://blog.expo.dev/the-new-expo-cli-f4250d8e3421 
https://docs.expo.dev/more/expo-cli/
